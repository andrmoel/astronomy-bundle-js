export type Libration = {
    lon: number,
    lat: number,
};

export type Quantities = {
    rho: number,
    sigma: number,
    tau: number,
};

export type WandA = {
    W: number,
    A: number,
};
